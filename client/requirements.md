## Packages
framer-motion | Smooth animations for page transitions and card interactions
react-hook-form | Form state management
@hookform/resolvers | Zod resolver for react-hook-form
clsx | Conditional class names
tailwind-merge | Merging tailwind classes safely

## Notes
- Theme: Agricultural/Nature (Greens, Earth tones)
- Font: 'Outfit' for headings, 'DM Sans' for body
- Integration: Uses /api/profile, /api/schemes, /api/recommendations/generate
- Auth: Uses /api/auth/user (already implemented in base)
